# SigningSignatoryRole

## Example Usage

```java
import to.unified.unified_java_sdk.models.shared.SigningSignatoryRole;

SigningSignatoryRole value = SigningSignatoryRole.SIGNER;

// Open enum: use .of() to create instances from custom string values
SigningSignatoryRole custom = SigningSignatoryRole.of("custom_value");
```


## Values

| Name               | Value              |
| ------------------ | ------------------ |
| `SIGNER`           | SIGNER             |
| `CC`               | CC                 |
| `APPROVER`         | APPROVER           |
| `IN_PERSON_SIGNER` | IN_PERSON_SIGNER   |
| `VIEWER`           | VIEWER             |