# ListSpaceId

## Example Usage

```java
import to.unified.unified_java_sdk.models.shared.ListSpaceId;

ListSpaceId value = ListSpaceId.SUPPORTED_REQUIRED;

// Open enum: use .of() to create instances from custom string values
ListSpaceId custom = ListSpaceId.of("custom_value");
```


## Values

| Name                 | Value                |
| -------------------- | -------------------- |
| `SUPPORTED_REQUIRED` | supported-required   |
| `SUPPORTED`          | supported            |
| `NOT_SUPPORTED`      | not-supported        |