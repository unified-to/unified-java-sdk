# ComputeMode

## Example Usage

```java
import to.unified.unified_java_sdk.models.shared.ComputeMode;

ComputeMode value = ComputeMode.REALTIME;

// Open enum: use .of() to create instances from custom string values
ComputeMode custom = ComputeMode.of("custom_value");
```


## Values

| Name       | Value      |
| ---------- | ---------- |
| `REALTIME` | REALTIME   |
| `BATCH`    | BATCH      |