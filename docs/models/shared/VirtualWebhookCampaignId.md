# VirtualWebhookCampaignId

## Example Usage

```java
import to.unified.unified_java_sdk.models.shared.VirtualWebhookCampaignId;

VirtualWebhookCampaignId value = VirtualWebhookCampaignId.SUPPORTED_REQUIRED;

// Open enum: use .of() to create instances from custom string values
VirtualWebhookCampaignId custom = VirtualWebhookCampaignId.of("custom_value");
```


## Values

| Name                 | Value                |
| -------------------- | -------------------- |
| `SUPPORTED_REQUIRED` | supported-required   |
| `SUPPORTED`          | supported            |
| `NOT_SUPPORTED`      | not-supported        |