/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package to.unified.unified_java_sdk.models.shared;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class ListMedium {

    public static final ListMedium SUPPORTED_REQUIRED = new ListMedium("supported-required");
    public static final ListMedium SUPPORTED = new ListMedium("supported");
    public static final ListMedium NOT_SUPPORTED = new ListMedium("not-supported");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, ListMedium> values = createValuesMap();
    private static final Map<String, ListMediumEnum> enums = createEnumsMap();

    private final String value;

    private ListMedium(String value) {
        this.value = value;
    }

    /**
     * Returns a ListMedium with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as ListMedium
     */ 
    @JsonCreator
    public static ListMedium of(String value) {
        synchronized (ListMedium.class) {
            return values.computeIfAbsent(value, v -> new ListMedium(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<ListMediumEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        ListMedium other = (ListMedium) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "ListMedium [value=" + value + "]";
    }

    // return an array just like an enum
    public static ListMedium[] values() {
        synchronized (ListMedium.class) {
            return values.values().toArray(new ListMedium[] {});
        }
    }

    private static final Map<String, ListMedium> createValuesMap() {
        Map<String, ListMedium> map = new LinkedHashMap<>();
        map.put("supported-required", SUPPORTED_REQUIRED);
        map.put("supported", SUPPORTED);
        map.put("not-supported", NOT_SUPPORTED);
        return map;
    }

    private static final Map<String, ListMediumEnum> createEnumsMap() {
        Map<String, ListMediumEnum> map = new HashMap<>();
        map.put("supported-required", ListMediumEnum.SUPPORTED_REQUIRED);
        map.put("supported", ListMediumEnum.SUPPORTED);
        map.put("not-supported", ListMediumEnum.NOT_SUPPORTED);
        return map;
    }
    
    
    public enum ListMediumEnum {

        SUPPORTED_REQUIRED("supported-required"),
        SUPPORTED("supported"),
        NOT_SUPPORTED("not-supported"),;

        private final String value;

        private ListMediumEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

