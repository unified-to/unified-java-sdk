/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package to.unified.unified_java_sdk.models.shared;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class ListPackageId {

    public static final ListPackageId SUPPORTED_REQUIRED = new ListPackageId("supported-required");
    public static final ListPackageId SUPPORTED = new ListPackageId("supported");
    public static final ListPackageId NOT_SUPPORTED = new ListPackageId("not-supported");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, ListPackageId> values = createValuesMap();
    private static final Map<String, ListPackageIdEnum> enums = createEnumsMap();

    private final String value;

    private ListPackageId(String value) {
        this.value = value;
    }

    /**
     * Returns a ListPackageId with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as ListPackageId
     */ 
    @JsonCreator
    public static ListPackageId of(String value) {
        synchronized (ListPackageId.class) {
            return values.computeIfAbsent(value, v -> new ListPackageId(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<ListPackageIdEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        ListPackageId other = (ListPackageId) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "ListPackageId [value=" + value + "]";
    }

    // return an array just like an enum
    public static ListPackageId[] values() {
        synchronized (ListPackageId.class) {
            return values.values().toArray(new ListPackageId[] {});
        }
    }

    private static final Map<String, ListPackageId> createValuesMap() {
        Map<String, ListPackageId> map = new LinkedHashMap<>();
        map.put("supported-required", SUPPORTED_REQUIRED);
        map.put("supported", SUPPORTED);
        map.put("not-supported", NOT_SUPPORTED);
        return map;
    }

    private static final Map<String, ListPackageIdEnum> createEnumsMap() {
        Map<String, ListPackageIdEnum> map = new HashMap<>();
        map.put("supported-required", ListPackageIdEnum.SUPPORTED_REQUIRED);
        map.put("supported", ListPackageIdEnum.SUPPORTED);
        map.put("not-supported", ListPackageIdEnum.NOT_SUPPORTED);
        return map;
    }
    
    
    public enum ListPackageIdEnum {

        SUPPORTED_REQUIRED("supported-required"),
        SUPPORTED("supported"),
        NOT_SUPPORTED("not-supported"),;

        private final String value;

        private ListPackageIdEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

