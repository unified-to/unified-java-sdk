/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package to.unified.unified_java_sdk.models.shared;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class Tax {

    public static final Tax PRE_TAX = new Tax("PRE_TAX");
    public static final Tax POST_TAX = new Tax("POST_TAX");
    public static final Tax TAXABLE = new Tax("TAXABLE");
    public static final Tax NON_TAXABLE = new Tax("NON_TAXABLE");
    public static final Tax TAX = new Tax("TAX");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, Tax> values = createValuesMap();
    private static final Map<String, TaxEnum> enums = createEnumsMap();

    private final String value;

    private Tax(String value) {
        this.value = value;
    }

    /**
     * Returns a Tax with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as Tax
     */ 
    @JsonCreator
    public static Tax of(String value) {
        synchronized (Tax.class) {
            return values.computeIfAbsent(value, v -> new Tax(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<TaxEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        Tax other = (Tax) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "Tax [value=" + value + "]";
    }

    // return an array just like an enum
    public static Tax[] values() {
        synchronized (Tax.class) {
            return values.values().toArray(new Tax[] {});
        }
    }

    private static final Map<String, Tax> createValuesMap() {
        Map<String, Tax> map = new LinkedHashMap<>();
        map.put("PRE_TAX", PRE_TAX);
        map.put("POST_TAX", POST_TAX);
        map.put("TAXABLE", TAXABLE);
        map.put("NON_TAXABLE", NON_TAXABLE);
        map.put("TAX", TAX);
        return map;
    }

    private static final Map<String, TaxEnum> createEnumsMap() {
        Map<String, TaxEnum> map = new HashMap<>();
        map.put("PRE_TAX", TaxEnum.PRE_TAX);
        map.put("POST_TAX", TaxEnum.POST_TAX);
        map.put("TAXABLE", TaxEnum.TAXABLE);
        map.put("NON_TAXABLE", TaxEnum.NON_TAXABLE);
        map.put("TAX", TaxEnum.TAX);
        return map;
    }
    
    
    public enum TaxEnum {

        PRE_TAX("PRE_TAX"),
        POST_TAX("POST_TAX"),
        TAXABLE("TAXABLE"),
        NON_TAXABLE("NON_TAXABLE"),
        TAX("TAX"),;

        private final String value;

        private TaxEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

