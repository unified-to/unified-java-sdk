/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package to.unified.unified_java_sdk.models.shared;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class NativeWebhookListId {

    public static final NativeWebhookListId SUPPORTED_REQUIRED = new NativeWebhookListId("supported-required");
    public static final NativeWebhookListId SUPPORTED = new NativeWebhookListId("supported");
    public static final NativeWebhookListId NOT_SUPPORTED = new NativeWebhookListId("not-supported");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, NativeWebhookListId> values = createValuesMap();
    private static final Map<String, NativeWebhookListIdEnum> enums = createEnumsMap();

    private final String value;

    private NativeWebhookListId(String value) {
        this.value = value;
    }

    /**
     * Returns a NativeWebhookListId with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as NativeWebhookListId
     */ 
    @JsonCreator
    public static NativeWebhookListId of(String value) {
        synchronized (NativeWebhookListId.class) {
            return values.computeIfAbsent(value, v -> new NativeWebhookListId(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<NativeWebhookListIdEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        NativeWebhookListId other = (NativeWebhookListId) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "NativeWebhookListId [value=" + value + "]";
    }

    // return an array just like an enum
    public static NativeWebhookListId[] values() {
        synchronized (NativeWebhookListId.class) {
            return values.values().toArray(new NativeWebhookListId[] {});
        }
    }

    private static final Map<String, NativeWebhookListId> createValuesMap() {
        Map<String, NativeWebhookListId> map = new LinkedHashMap<>();
        map.put("supported-required", SUPPORTED_REQUIRED);
        map.put("supported", SUPPORTED);
        map.put("not-supported", NOT_SUPPORTED);
        return map;
    }

    private static final Map<String, NativeWebhookListIdEnum> createEnumsMap() {
        Map<String, NativeWebhookListIdEnum> map = new HashMap<>();
        map.put("supported-required", NativeWebhookListIdEnum.SUPPORTED_REQUIRED);
        map.put("supported", NativeWebhookListIdEnum.SUPPORTED);
        map.put("not-supported", NativeWebhookListIdEnum.NOT_SUPPORTED);
        return map;
    }
    
    
    public enum NativeWebhookListIdEnum {

        SUPPORTED_REQUIRED("supported-required"),
        SUPPORTED("supported"),
        NOT_SUPPORTED("not-supported"),;

        private final String value;

        private NativeWebhookListIdEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

