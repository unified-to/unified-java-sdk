/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package to.unified.unified_java_sdk.models.shared;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class AtsCompensationType {

    public static final AtsCompensationType SALARY = new AtsCompensationType("SALARY");
    public static final AtsCompensationType BONUS = new AtsCompensationType("BONUS");
    public static final AtsCompensationType STOCK_OPTIONS = new AtsCompensationType("STOCK_OPTIONS");
    public static final AtsCompensationType EQUITY = new AtsCompensationType("EQUITY");
    public static final AtsCompensationType OTHER = new AtsCompensationType("OTHER");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, AtsCompensationType> values = createValuesMap();
    private static final Map<String, AtsCompensationTypeEnum> enums = createEnumsMap();

    private final String value;

    private AtsCompensationType(String value) {
        this.value = value;
    }

    /**
     * Returns a AtsCompensationType with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as AtsCompensationType
     */ 
    @JsonCreator
    public static AtsCompensationType of(String value) {
        synchronized (AtsCompensationType.class) {
            return values.computeIfAbsent(value, v -> new AtsCompensationType(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<AtsCompensationTypeEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        AtsCompensationType other = (AtsCompensationType) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "AtsCompensationType [value=" + value + "]";
    }

    // return an array just like an enum
    public static AtsCompensationType[] values() {
        synchronized (AtsCompensationType.class) {
            return values.values().toArray(new AtsCompensationType[] {});
        }
    }

    private static final Map<String, AtsCompensationType> createValuesMap() {
        Map<String, AtsCompensationType> map = new LinkedHashMap<>();
        map.put("SALARY", SALARY);
        map.put("BONUS", BONUS);
        map.put("STOCK_OPTIONS", STOCK_OPTIONS);
        map.put("EQUITY", EQUITY);
        map.put("OTHER", OTHER);
        return map;
    }

    private static final Map<String, AtsCompensationTypeEnum> createEnumsMap() {
        Map<String, AtsCompensationTypeEnum> map = new HashMap<>();
        map.put("SALARY", AtsCompensationTypeEnum.SALARY);
        map.put("BONUS", AtsCompensationTypeEnum.BONUS);
        map.put("STOCK_OPTIONS", AtsCompensationTypeEnum.STOCK_OPTIONS);
        map.put("EQUITY", AtsCompensationTypeEnum.EQUITY);
        map.put("OTHER", AtsCompensationTypeEnum.OTHER);
        return map;
    }
    
    
    public enum AtsCompensationTypeEnum {

        SALARY("SALARY"),
        BONUS("BONUS"),
        STOCK_OPTIONS("STOCK_OPTIONS"),
        EQUITY("EQUITY"),
        OTHER("OTHER"),;

        private final String value;

        private AtsCompensationTypeEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

