/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package to.unified.unified_java_sdk.models.shared;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class NativeWebhookOrderId {

    public static final NativeWebhookOrderId SUPPORTED_REQUIRED = new NativeWebhookOrderId("supported-required");
    public static final NativeWebhookOrderId SUPPORTED = new NativeWebhookOrderId("supported");
    public static final NativeWebhookOrderId NOT_SUPPORTED = new NativeWebhookOrderId("not-supported");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, NativeWebhookOrderId> values = createValuesMap();
    private static final Map<String, NativeWebhookOrderIdEnum> enums = createEnumsMap();

    private final String value;

    private NativeWebhookOrderId(String value) {
        this.value = value;
    }

    /**
     * Returns a NativeWebhookOrderId with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as NativeWebhookOrderId
     */ 
    @JsonCreator
    public static NativeWebhookOrderId of(String value) {
        synchronized (NativeWebhookOrderId.class) {
            return values.computeIfAbsent(value, v -> new NativeWebhookOrderId(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<NativeWebhookOrderIdEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        NativeWebhookOrderId other = (NativeWebhookOrderId) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "NativeWebhookOrderId [value=" + value + "]";
    }

    // return an array just like an enum
    public static NativeWebhookOrderId[] values() {
        synchronized (NativeWebhookOrderId.class) {
            return values.values().toArray(new NativeWebhookOrderId[] {});
        }
    }

    private static final Map<String, NativeWebhookOrderId> createValuesMap() {
        Map<String, NativeWebhookOrderId> map = new LinkedHashMap<>();
        map.put("supported-required", SUPPORTED_REQUIRED);
        map.put("supported", SUPPORTED);
        map.put("not-supported", NOT_SUPPORTED);
        return map;
    }

    private static final Map<String, NativeWebhookOrderIdEnum> createEnumsMap() {
        Map<String, NativeWebhookOrderIdEnum> map = new HashMap<>();
        map.put("supported-required", NativeWebhookOrderIdEnum.SUPPORTED_REQUIRED);
        map.put("supported", NativeWebhookOrderIdEnum.SUPPORTED);
        map.put("not-supported", NativeWebhookOrderIdEnum.NOT_SUPPORTED);
        return map;
    }
    
    
    public enum NativeWebhookOrderIdEnum {

        SUPPORTED_REQUIRED("supported-required"),
        SUPPORTED("supported"),
        NOT_SUPPORTED("not-supported"),;

        private final String value;

        private NativeWebhookOrderIdEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

