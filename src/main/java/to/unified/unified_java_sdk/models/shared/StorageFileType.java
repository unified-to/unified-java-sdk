/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package to.unified.unified_java_sdk.models.shared;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class StorageFileType {

    public static final StorageFileType FILE = new StorageFileType("FILE");
    public static final StorageFileType FOLDER = new StorageFileType("FOLDER");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, StorageFileType> values = createValuesMap();
    private static final Map<String, StorageFileTypeEnum> enums = createEnumsMap();

    private final String value;

    private StorageFileType(String value) {
        this.value = value;
    }

    /**
     * Returns a StorageFileType with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as StorageFileType
     */ 
    @JsonCreator
    public static StorageFileType of(String value) {
        synchronized (StorageFileType.class) {
            return values.computeIfAbsent(value, v -> new StorageFileType(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<StorageFileTypeEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        StorageFileType other = (StorageFileType) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "StorageFileType [value=" + value + "]";
    }

    // return an array just like an enum
    public static StorageFileType[] values() {
        synchronized (StorageFileType.class) {
            return values.values().toArray(new StorageFileType[] {});
        }
    }

    private static final Map<String, StorageFileType> createValuesMap() {
        Map<String, StorageFileType> map = new LinkedHashMap<>();
        map.put("FILE", FILE);
        map.put("FOLDER", FOLDER);
        return map;
    }

    private static final Map<String, StorageFileTypeEnum> createEnumsMap() {
        Map<String, StorageFileTypeEnum> map = new HashMap<>();
        map.put("FILE", StorageFileTypeEnum.FILE);
        map.put("FOLDER", StorageFileTypeEnum.FOLDER);
        return map;
    }
    
    
    public enum StorageFileTypeEnum {

        FILE("FILE"),
        FOLDER("FOLDER"),;

        private final String value;

        private StorageFileTypeEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

