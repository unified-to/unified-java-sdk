/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package to.unified.unified_java_sdk.models.shared;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class CreativeSelection {

    public static final CreativeSelection OPTIMIZED = new CreativeSelection("OPTIMIZED");
    public static final CreativeSelection ROUND_ROBIN = new CreativeSelection("ROUND_ROBIN");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, CreativeSelection> values = createValuesMap();
    private static final Map<String, CreativeSelectionEnum> enums = createEnumsMap();

    private final String value;

    private CreativeSelection(String value) {
        this.value = value;
    }

    /**
     * Returns a CreativeSelection with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as CreativeSelection
     */ 
    @JsonCreator
    public static CreativeSelection of(String value) {
        synchronized (CreativeSelection.class) {
            return values.computeIfAbsent(value, v -> new CreativeSelection(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<CreativeSelectionEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        CreativeSelection other = (CreativeSelection) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "CreativeSelection [value=" + value + "]";
    }

    // return an array just like an enum
    public static CreativeSelection[] values() {
        synchronized (CreativeSelection.class) {
            return values.values().toArray(new CreativeSelection[] {});
        }
    }

    private static final Map<String, CreativeSelection> createValuesMap() {
        Map<String, CreativeSelection> map = new LinkedHashMap<>();
        map.put("OPTIMIZED", OPTIMIZED);
        map.put("ROUND_ROBIN", ROUND_ROBIN);
        return map;
    }

    private static final Map<String, CreativeSelectionEnum> createEnumsMap() {
        Map<String, CreativeSelectionEnum> map = new HashMap<>();
        map.put("OPTIMIZED", CreativeSelectionEnum.OPTIMIZED);
        map.put("ROUND_ROBIN", CreativeSelectionEnum.ROUND_ROBIN);
        return map;
    }
    
    
    public enum CreativeSelectionEnum {

        OPTIMIZED("OPTIMIZED"),
        ROUND_ROBIN("ROUND_ROBIN"),;

        private final String value;

        private CreativeSelectionEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

