/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package to.unified.unified_java_sdk.models.shared;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class ListSegmentId {

    public static final ListSegmentId SUPPORTED_REQUIRED = new ListSegmentId("supported-required");
    public static final ListSegmentId SUPPORTED = new ListSegmentId("supported");
    public static final ListSegmentId NOT_SUPPORTED = new ListSegmentId("not-supported");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, ListSegmentId> values = createValuesMap();
    private static final Map<String, ListSegmentIdEnum> enums = createEnumsMap();

    private final String value;

    private ListSegmentId(String value) {
        this.value = value;
    }

    /**
     * Returns a ListSegmentId with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as ListSegmentId
     */ 
    @JsonCreator
    public static ListSegmentId of(String value) {
        synchronized (ListSegmentId.class) {
            return values.computeIfAbsent(value, v -> new ListSegmentId(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<ListSegmentIdEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        ListSegmentId other = (ListSegmentId) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "ListSegmentId [value=" + value + "]";
    }

    // return an array just like an enum
    public static ListSegmentId[] values() {
        synchronized (ListSegmentId.class) {
            return values.values().toArray(new ListSegmentId[] {});
        }
    }

    private static final Map<String, ListSegmentId> createValuesMap() {
        Map<String, ListSegmentId> map = new LinkedHashMap<>();
        map.put("supported-required", SUPPORTED_REQUIRED);
        map.put("supported", SUPPORTED);
        map.put("not-supported", NOT_SUPPORTED);
        return map;
    }

    private static final Map<String, ListSegmentIdEnum> createEnumsMap() {
        Map<String, ListSegmentIdEnum> map = new HashMap<>();
        map.put("supported-required", ListSegmentIdEnum.SUPPORTED_REQUIRED);
        map.put("supported", ListSegmentIdEnum.SUPPORTED);
        map.put("not-supported", ListSegmentIdEnum.NOT_SUPPORTED);
        return map;
    }
    
    
    public enum ListSegmentIdEnum {

        SUPPORTED_REQUIRED("supported-required"),
        SUPPORTED("supported"),
        NOT_SUPPORTED("not-supported"),;

        private final String value;

        private ListSegmentIdEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

