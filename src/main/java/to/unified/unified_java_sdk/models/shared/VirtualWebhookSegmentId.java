/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package to.unified.unified_java_sdk.models.shared;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class VirtualWebhookSegmentId {

    public static final VirtualWebhookSegmentId SUPPORTED_REQUIRED = new VirtualWebhookSegmentId("supported-required");
    public static final VirtualWebhookSegmentId SUPPORTED = new VirtualWebhookSegmentId("supported");
    public static final VirtualWebhookSegmentId NOT_SUPPORTED = new VirtualWebhookSegmentId("not-supported");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, VirtualWebhookSegmentId> values = createValuesMap();
    private static final Map<String, VirtualWebhookSegmentIdEnum> enums = createEnumsMap();

    private final String value;

    private VirtualWebhookSegmentId(String value) {
        this.value = value;
    }

    /**
     * Returns a VirtualWebhookSegmentId with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as VirtualWebhookSegmentId
     */ 
    @JsonCreator
    public static VirtualWebhookSegmentId of(String value) {
        synchronized (VirtualWebhookSegmentId.class) {
            return values.computeIfAbsent(value, v -> new VirtualWebhookSegmentId(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<VirtualWebhookSegmentIdEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        VirtualWebhookSegmentId other = (VirtualWebhookSegmentId) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "VirtualWebhookSegmentId [value=" + value + "]";
    }

    // return an array just like an enum
    public static VirtualWebhookSegmentId[] values() {
        synchronized (VirtualWebhookSegmentId.class) {
            return values.values().toArray(new VirtualWebhookSegmentId[] {});
        }
    }

    private static final Map<String, VirtualWebhookSegmentId> createValuesMap() {
        Map<String, VirtualWebhookSegmentId> map = new LinkedHashMap<>();
        map.put("supported-required", SUPPORTED_REQUIRED);
        map.put("supported", SUPPORTED);
        map.put("not-supported", NOT_SUPPORTED);
        return map;
    }

    private static final Map<String, VirtualWebhookSegmentIdEnum> createEnumsMap() {
        Map<String, VirtualWebhookSegmentIdEnum> map = new HashMap<>();
        map.put("supported-required", VirtualWebhookSegmentIdEnum.SUPPORTED_REQUIRED);
        map.put("supported", VirtualWebhookSegmentIdEnum.SUPPORTED);
        map.put("not-supported", VirtualWebhookSegmentIdEnum.NOT_SUPPORTED);
        return map;
    }
    
    
    public enum VirtualWebhookSegmentIdEnum {

        SUPPORTED_REQUIRED("supported-required"),
        SUPPORTED("supported"),
        NOT_SUPPORTED("not-supported"),;

        private final String value;

        private VirtualWebhookSegmentIdEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

