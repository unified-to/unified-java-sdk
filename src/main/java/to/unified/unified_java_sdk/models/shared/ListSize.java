/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package to.unified.unified_java_sdk.models.shared;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class ListSize {

    public static final ListSize SUPPORTED_REQUIRED = new ListSize("supported-required");
    public static final ListSize SUPPORTED = new ListSize("supported");
    public static final ListSize NOT_SUPPORTED = new ListSize("not-supported");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, ListSize> values = createValuesMap();
    private static final Map<String, ListSizeEnum> enums = createEnumsMap();

    private final String value;

    private ListSize(String value) {
        this.value = value;
    }

    /**
     * Returns a ListSize with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as ListSize
     */ 
    @JsonCreator
    public static ListSize of(String value) {
        synchronized (ListSize.class) {
            return values.computeIfAbsent(value, v -> new ListSize(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<ListSizeEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        ListSize other = (ListSize) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "ListSize [value=" + value + "]";
    }

    // return an array just like an enum
    public static ListSize[] values() {
        synchronized (ListSize.class) {
            return values.values().toArray(new ListSize[] {});
        }
    }

    private static final Map<String, ListSize> createValuesMap() {
        Map<String, ListSize> map = new LinkedHashMap<>();
        map.put("supported-required", SUPPORTED_REQUIRED);
        map.put("supported", SUPPORTED);
        map.put("not-supported", NOT_SUPPORTED);
        return map;
    }

    private static final Map<String, ListSizeEnum> createEnumsMap() {
        Map<String, ListSizeEnum> map = new HashMap<>();
        map.put("supported-required", ListSizeEnum.SUPPORTED_REQUIRED);
        map.put("supported", ListSizeEnum.SUPPORTED);
        map.put("not-supported", ListSizeEnum.NOT_SUPPORTED);
        return map;
    }
    
    
    public enum ListSizeEnum {

        SUPPORTED_REQUIRED("supported-required"),
        SUPPORTED("supported"),
        NOT_SUPPORTED("not-supported"),;

        private final String value;

        private ListSizeEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

