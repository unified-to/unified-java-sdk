/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package to.unified.unified_java_sdk.models.shared;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class VirtualWebhookStudentId {

    public static final VirtualWebhookStudentId SUPPORTED_REQUIRED = new VirtualWebhookStudentId("supported-required");
    public static final VirtualWebhookStudentId SUPPORTED = new VirtualWebhookStudentId("supported");
    public static final VirtualWebhookStudentId NOT_SUPPORTED = new VirtualWebhookStudentId("not-supported");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, VirtualWebhookStudentId> values = createValuesMap();
    private static final Map<String, VirtualWebhookStudentIdEnum> enums = createEnumsMap();

    private final String value;

    private VirtualWebhookStudentId(String value) {
        this.value = value;
    }

    /**
     * Returns a VirtualWebhookStudentId with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as VirtualWebhookStudentId
     */ 
    @JsonCreator
    public static VirtualWebhookStudentId of(String value) {
        synchronized (VirtualWebhookStudentId.class) {
            return values.computeIfAbsent(value, v -> new VirtualWebhookStudentId(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<VirtualWebhookStudentIdEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        VirtualWebhookStudentId other = (VirtualWebhookStudentId) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "VirtualWebhookStudentId [value=" + value + "]";
    }

    // return an array just like an enum
    public static VirtualWebhookStudentId[] values() {
        synchronized (VirtualWebhookStudentId.class) {
            return values.values().toArray(new VirtualWebhookStudentId[] {});
        }
    }

    private static final Map<String, VirtualWebhookStudentId> createValuesMap() {
        Map<String, VirtualWebhookStudentId> map = new LinkedHashMap<>();
        map.put("supported-required", SUPPORTED_REQUIRED);
        map.put("supported", SUPPORTED);
        map.put("not-supported", NOT_SUPPORTED);
        return map;
    }

    private static final Map<String, VirtualWebhookStudentIdEnum> createEnumsMap() {
        Map<String, VirtualWebhookStudentIdEnum> map = new HashMap<>();
        map.put("supported-required", VirtualWebhookStudentIdEnum.SUPPORTED_REQUIRED);
        map.put("supported", VirtualWebhookStudentIdEnum.SUPPORTED);
        map.put("not-supported", VirtualWebhookStudentIdEnum.NOT_SUPPORTED);
        return map;
    }
    
    
    public enum VirtualWebhookStudentIdEnum {

        SUPPORTED_REQUIRED("supported-required"),
        SUPPORTED("supported"),
        NOT_SUPPORTED("not-supported"),;

        private final String value;

        private VirtualWebhookStudentIdEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

