/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package to.unified.unified_java_sdk.models.shared;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class VirtualWebhookApproverUserId {

    public static final VirtualWebhookApproverUserId SUPPORTED_REQUIRED = new VirtualWebhookApproverUserId("supported-required");
    public static final VirtualWebhookApproverUserId SUPPORTED = new VirtualWebhookApproverUserId("supported");
    public static final VirtualWebhookApproverUserId NOT_SUPPORTED = new VirtualWebhookApproverUserId("not-supported");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, VirtualWebhookApproverUserId> values = createValuesMap();
    private static final Map<String, VirtualWebhookApproverUserIdEnum> enums = createEnumsMap();

    private final String value;

    private VirtualWebhookApproverUserId(String value) {
        this.value = value;
    }

    /**
     * Returns a VirtualWebhookApproverUserId with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as VirtualWebhookApproverUserId
     */ 
    @JsonCreator
    public static VirtualWebhookApproverUserId of(String value) {
        synchronized (VirtualWebhookApproverUserId.class) {
            return values.computeIfAbsent(value, v -> new VirtualWebhookApproverUserId(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<VirtualWebhookApproverUserIdEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        VirtualWebhookApproverUserId other = (VirtualWebhookApproverUserId) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "VirtualWebhookApproverUserId [value=" + value + "]";
    }

    // return an array just like an enum
    public static VirtualWebhookApproverUserId[] values() {
        synchronized (VirtualWebhookApproverUserId.class) {
            return values.values().toArray(new VirtualWebhookApproverUserId[] {});
        }
    }

    private static final Map<String, VirtualWebhookApproverUserId> createValuesMap() {
        Map<String, VirtualWebhookApproverUserId> map = new LinkedHashMap<>();
        map.put("supported-required", SUPPORTED_REQUIRED);
        map.put("supported", SUPPORTED);
        map.put("not-supported", NOT_SUPPORTED);
        return map;
    }

    private static final Map<String, VirtualWebhookApproverUserIdEnum> createEnumsMap() {
        Map<String, VirtualWebhookApproverUserIdEnum> map = new HashMap<>();
        map.put("supported-required", VirtualWebhookApproverUserIdEnum.SUPPORTED_REQUIRED);
        map.put("supported", VirtualWebhookApproverUserIdEnum.SUPPORTED);
        map.put("not-supported", VirtualWebhookApproverUserIdEnum.NOT_SUPPORTED);
        return map;
    }
    
    
    public enum VirtualWebhookApproverUserIdEnum {

        SUPPORTED_REQUIRED("supported-required"),
        SUPPORTED("supported"),
        NOT_SUPPORTED("not-supported"),;

        private final String value;

        private VirtualWebhookApproverUserIdEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

