/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package to.unified.unified_java_sdk.models.shared;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class Category {

    public static final Category STANDARD = new Category("STANDARD");
    public static final Category DATE_DRIVEN = new Category("DATE_DRIVEN");
    public static final Category OTHER = new Category("OTHER");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, Category> values = createValuesMap();
    private static final Map<String, CategoryEnum> enums = createEnumsMap();

    private final String value;

    private Category(String value) {
        this.value = value;
    }

    /**
     * Returns a Category with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as Category
     */ 
    @JsonCreator
    public static Category of(String value) {
        synchronized (Category.class) {
            return values.computeIfAbsent(value, v -> new Category(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<CategoryEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        Category other = (Category) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "Category [value=" + value + "]";
    }

    // return an array just like an enum
    public static Category[] values() {
        synchronized (Category.class) {
            return values.values().toArray(new Category[] {});
        }
    }

    private static final Map<String, Category> createValuesMap() {
        Map<String, Category> map = new LinkedHashMap<>();
        map.put("STANDARD", STANDARD);
        map.put("DATE_DRIVEN", DATE_DRIVEN);
        map.put("OTHER", OTHER);
        return map;
    }

    private static final Map<String, CategoryEnum> createEnumsMap() {
        Map<String, CategoryEnum> map = new HashMap<>();
        map.put("STANDARD", CategoryEnum.STANDARD);
        map.put("DATE_DRIVEN", CategoryEnum.DATE_DRIVEN);
        map.put("OTHER", CategoryEnum.OTHER);
        return map;
    }
    
    
    public enum CategoryEnum {

        STANDARD("STANDARD"),
        DATE_DRIVEN("DATE_DRIVEN"),
        OTHER("OTHER"),;

        private final String value;

        private CategoryEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

