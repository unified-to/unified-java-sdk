/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package to.unified.unified_java_sdk.models.shared;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class VirtualWebhookStartGte {

    public static final VirtualWebhookStartGte SUPPORTED_REQUIRED = new VirtualWebhookStartGte("supported-required");
    public static final VirtualWebhookStartGte SUPPORTED = new VirtualWebhookStartGte("supported");
    public static final VirtualWebhookStartGte NOT_SUPPORTED = new VirtualWebhookStartGte("not-supported");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, VirtualWebhookStartGte> values = createValuesMap();
    private static final Map<String, VirtualWebhookStartGteEnum> enums = createEnumsMap();

    private final String value;

    private VirtualWebhookStartGte(String value) {
        this.value = value;
    }

    /**
     * Returns a VirtualWebhookStartGte with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as VirtualWebhookStartGte
     */ 
    @JsonCreator
    public static VirtualWebhookStartGte of(String value) {
        synchronized (VirtualWebhookStartGte.class) {
            return values.computeIfAbsent(value, v -> new VirtualWebhookStartGte(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<VirtualWebhookStartGteEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        VirtualWebhookStartGte other = (VirtualWebhookStartGte) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "VirtualWebhookStartGte [value=" + value + "]";
    }

    // return an array just like an enum
    public static VirtualWebhookStartGte[] values() {
        synchronized (VirtualWebhookStartGte.class) {
            return values.values().toArray(new VirtualWebhookStartGte[] {});
        }
    }

    private static final Map<String, VirtualWebhookStartGte> createValuesMap() {
        Map<String, VirtualWebhookStartGte> map = new LinkedHashMap<>();
        map.put("supported-required", SUPPORTED_REQUIRED);
        map.put("supported", SUPPORTED);
        map.put("not-supported", NOT_SUPPORTED);
        return map;
    }

    private static final Map<String, VirtualWebhookStartGteEnum> createEnumsMap() {
        Map<String, VirtualWebhookStartGteEnum> map = new HashMap<>();
        map.put("supported-required", VirtualWebhookStartGteEnum.SUPPORTED_REQUIRED);
        map.put("supported", VirtualWebhookStartGteEnum.SUPPORTED);
        map.put("not-supported", VirtualWebhookStartGteEnum.NOT_SUPPORTED);
        return map;
    }
    
    
    public enum VirtualWebhookStartGteEnum {

        SUPPORTED_REQUIRED("supported-required"),
        SUPPORTED("supported"),
        NOT_SUPPORTED("not-supported"),;

        private final String value;

        private VirtualWebhookStartGteEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

