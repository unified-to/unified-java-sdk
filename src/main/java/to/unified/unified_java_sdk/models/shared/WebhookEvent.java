/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package to.unified.unified_java_sdk.models.shared;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class WebhookEvent {

    public static final WebhookEvent UPDATED = new WebhookEvent("updated");
    public static final WebhookEvent CREATED = new WebhookEvent("created");
    public static final WebhookEvent DELETED = new WebhookEvent("deleted");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, WebhookEvent> values = createValuesMap();
    private static final Map<String, WebhookEventEnum> enums = createEnumsMap();

    private final String value;

    private WebhookEvent(String value) {
        this.value = value;
    }

    /**
     * Returns a WebhookEvent with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as WebhookEvent
     */ 
    @JsonCreator
    public static WebhookEvent of(String value) {
        synchronized (WebhookEvent.class) {
            return values.computeIfAbsent(value, v -> new WebhookEvent(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<WebhookEventEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        WebhookEvent other = (WebhookEvent) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "WebhookEvent [value=" + value + "]";
    }

    // return an array just like an enum
    public static WebhookEvent[] values() {
        synchronized (WebhookEvent.class) {
            return values.values().toArray(new WebhookEvent[] {});
        }
    }

    private static final Map<String, WebhookEvent> createValuesMap() {
        Map<String, WebhookEvent> map = new LinkedHashMap<>();
        map.put("updated", UPDATED);
        map.put("created", CREATED);
        map.put("deleted", DELETED);
        return map;
    }

    private static final Map<String, WebhookEventEnum> createEnumsMap() {
        Map<String, WebhookEventEnum> map = new HashMap<>();
        map.put("updated", WebhookEventEnum.UPDATED);
        map.put("created", WebhookEventEnum.CREATED);
        map.put("deleted", WebhookEventEnum.DELETED);
        return map;
    }
    
    
    public enum WebhookEventEnum {

        UPDATED("updated"),
        CREATED("created"),
        DELETED("deleted"),;

        private final String value;

        private WebhookEventEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

