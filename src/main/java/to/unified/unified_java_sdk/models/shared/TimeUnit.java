/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package to.unified.unified_java_sdk.models.shared;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class TimeUnit {

    public static final TimeUnit UNSPECIFIED = new TimeUnit("UNSPECIFIED");
    public static final TimeUnit LIFETIME = new TimeUnit("LIFETIME");
    public static final TimeUnit MONTHS = new TimeUnit("MONTHS");
    public static final TimeUnit WEEKS = new TimeUnit("WEEKS");
    public static final TimeUnit DAYS = new TimeUnit("DAYS");
    public static final TimeUnit HOURS = new TimeUnit("HOURS");
    public static final TimeUnit MINUTES = new TimeUnit("MINUTES");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, TimeUnit> values = createValuesMap();
    private static final Map<String, TimeUnitEnum> enums = createEnumsMap();

    private final String value;

    private TimeUnit(String value) {
        this.value = value;
    }

    /**
     * Returns a TimeUnit with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as TimeUnit
     */ 
    @JsonCreator
    public static TimeUnit of(String value) {
        synchronized (TimeUnit.class) {
            return values.computeIfAbsent(value, v -> new TimeUnit(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<TimeUnitEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        TimeUnit other = (TimeUnit) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "TimeUnit [value=" + value + "]";
    }

    // return an array just like an enum
    public static TimeUnit[] values() {
        synchronized (TimeUnit.class) {
            return values.values().toArray(new TimeUnit[] {});
        }
    }

    private static final Map<String, TimeUnit> createValuesMap() {
        Map<String, TimeUnit> map = new LinkedHashMap<>();
        map.put("UNSPECIFIED", UNSPECIFIED);
        map.put("LIFETIME", LIFETIME);
        map.put("MONTHS", MONTHS);
        map.put("WEEKS", WEEKS);
        map.put("DAYS", DAYS);
        map.put("HOURS", HOURS);
        map.put("MINUTES", MINUTES);
        return map;
    }

    private static final Map<String, TimeUnitEnum> createEnumsMap() {
        Map<String, TimeUnitEnum> map = new HashMap<>();
        map.put("UNSPECIFIED", TimeUnitEnum.UNSPECIFIED);
        map.put("LIFETIME", TimeUnitEnum.LIFETIME);
        map.put("MONTHS", TimeUnitEnum.MONTHS);
        map.put("WEEKS", TimeUnitEnum.WEEKS);
        map.put("DAYS", TimeUnitEnum.DAYS);
        map.put("HOURS", TimeUnitEnum.HOURS);
        map.put("MINUTES", TimeUnitEnum.MINUTES);
        return map;
    }
    
    
    public enum TimeUnitEnum {

        UNSPECIFIED("UNSPECIFIED"),
        LIFETIME("LIFETIME"),
        MONTHS("MONTHS"),
        WEEKS("WEEKS"),
        DAYS("DAYS"),
        HOURS("HOURS"),
        MINUTES("MINUTES"),;

        private final String value;

        private TimeUnitEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

