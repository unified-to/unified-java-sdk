/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package to.unified.unified_java_sdk.models.shared;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class NativeWebhookCalendarId {

    public static final NativeWebhookCalendarId SUPPORTED_REQUIRED = new NativeWebhookCalendarId("supported-required");
    public static final NativeWebhookCalendarId SUPPORTED = new NativeWebhookCalendarId("supported");
    public static final NativeWebhookCalendarId NOT_SUPPORTED = new NativeWebhookCalendarId("not-supported");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, NativeWebhookCalendarId> values = createValuesMap();
    private static final Map<String, NativeWebhookCalendarIdEnum> enums = createEnumsMap();

    private final String value;

    private NativeWebhookCalendarId(String value) {
        this.value = value;
    }

    /**
     * Returns a NativeWebhookCalendarId with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as NativeWebhookCalendarId
     */ 
    @JsonCreator
    public static NativeWebhookCalendarId of(String value) {
        synchronized (NativeWebhookCalendarId.class) {
            return values.computeIfAbsent(value, v -> new NativeWebhookCalendarId(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<NativeWebhookCalendarIdEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        NativeWebhookCalendarId other = (NativeWebhookCalendarId) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "NativeWebhookCalendarId [value=" + value + "]";
    }

    // return an array just like an enum
    public static NativeWebhookCalendarId[] values() {
        synchronized (NativeWebhookCalendarId.class) {
            return values.values().toArray(new NativeWebhookCalendarId[] {});
        }
    }

    private static final Map<String, NativeWebhookCalendarId> createValuesMap() {
        Map<String, NativeWebhookCalendarId> map = new LinkedHashMap<>();
        map.put("supported-required", SUPPORTED_REQUIRED);
        map.put("supported", SUPPORTED);
        map.put("not-supported", NOT_SUPPORTED);
        return map;
    }

    private static final Map<String, NativeWebhookCalendarIdEnum> createEnumsMap() {
        Map<String, NativeWebhookCalendarIdEnum> map = new HashMap<>();
        map.put("supported-required", NativeWebhookCalendarIdEnum.SUPPORTED_REQUIRED);
        map.put("supported", NativeWebhookCalendarIdEnum.SUPPORTED);
        map.put("not-supported", NativeWebhookCalendarIdEnum.NOT_SUPPORTED);
        return map;
    }
    
    
    public enum NativeWebhookCalendarIdEnum {

        SUPPORTED_REQUIRED("supported-required"),
        SUPPORTED("supported"),
        NOT_SUPPORTED("not-supported"),;

        private final String value;

        private NativeWebhookCalendarIdEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

