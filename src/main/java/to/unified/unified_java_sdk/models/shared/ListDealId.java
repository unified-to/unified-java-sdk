/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package to.unified.unified_java_sdk.models.shared;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class ListDealId {

    public static final ListDealId SUPPORTED_REQUIRED = new ListDealId("supported-required");
    public static final ListDealId SUPPORTED = new ListDealId("supported");
    public static final ListDealId NOT_SUPPORTED = new ListDealId("not-supported");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, ListDealId> values = createValuesMap();
    private static final Map<String, ListDealIdEnum> enums = createEnumsMap();

    private final String value;

    private ListDealId(String value) {
        this.value = value;
    }

    /**
     * Returns a ListDealId with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as ListDealId
     */ 
    @JsonCreator
    public static ListDealId of(String value) {
        synchronized (ListDealId.class) {
            return values.computeIfAbsent(value, v -> new ListDealId(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<ListDealIdEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        ListDealId other = (ListDealId) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "ListDealId [value=" + value + "]";
    }

    // return an array just like an enum
    public static ListDealId[] values() {
        synchronized (ListDealId.class) {
            return values.values().toArray(new ListDealId[] {});
        }
    }

    private static final Map<String, ListDealId> createValuesMap() {
        Map<String, ListDealId> map = new LinkedHashMap<>();
        map.put("supported-required", SUPPORTED_REQUIRED);
        map.put("supported", SUPPORTED);
        map.put("not-supported", NOT_SUPPORTED);
        return map;
    }

    private static final Map<String, ListDealIdEnum> createEnumsMap() {
        Map<String, ListDealIdEnum> map = new HashMap<>();
        map.put("supported-required", ListDealIdEnum.SUPPORTED_REQUIRED);
        map.put("supported", ListDealIdEnum.SUPPORTED);
        map.put("not-supported", ListDealIdEnum.NOT_SUPPORTED);
        return map;
    }
    
    
    public enum ListDealIdEnum {

        SUPPORTED_REQUIRED("supported-required"),
        SUPPORTED("supported"),
        NOT_SUPPORTED("not-supported"),;

        private final String value;

        private ListDealIdEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

