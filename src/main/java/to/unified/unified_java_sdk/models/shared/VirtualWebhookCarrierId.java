/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package to.unified.unified_java_sdk.models.shared;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class VirtualWebhookCarrierId {

    public static final VirtualWebhookCarrierId SUPPORTED_REQUIRED = new VirtualWebhookCarrierId("supported-required");
    public static final VirtualWebhookCarrierId SUPPORTED = new VirtualWebhookCarrierId("supported");
    public static final VirtualWebhookCarrierId NOT_SUPPORTED = new VirtualWebhookCarrierId("not-supported");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, VirtualWebhookCarrierId> values = createValuesMap();
    private static final Map<String, VirtualWebhookCarrierIdEnum> enums = createEnumsMap();

    private final String value;

    private VirtualWebhookCarrierId(String value) {
        this.value = value;
    }

    /**
     * Returns a VirtualWebhookCarrierId with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as VirtualWebhookCarrierId
     */ 
    @JsonCreator
    public static VirtualWebhookCarrierId of(String value) {
        synchronized (VirtualWebhookCarrierId.class) {
            return values.computeIfAbsent(value, v -> new VirtualWebhookCarrierId(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<VirtualWebhookCarrierIdEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        VirtualWebhookCarrierId other = (VirtualWebhookCarrierId) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "VirtualWebhookCarrierId [value=" + value + "]";
    }

    // return an array just like an enum
    public static VirtualWebhookCarrierId[] values() {
        synchronized (VirtualWebhookCarrierId.class) {
            return values.values().toArray(new VirtualWebhookCarrierId[] {});
        }
    }

    private static final Map<String, VirtualWebhookCarrierId> createValuesMap() {
        Map<String, VirtualWebhookCarrierId> map = new LinkedHashMap<>();
        map.put("supported-required", SUPPORTED_REQUIRED);
        map.put("supported", SUPPORTED);
        map.put("not-supported", NOT_SUPPORTED);
        return map;
    }

    private static final Map<String, VirtualWebhookCarrierIdEnum> createEnumsMap() {
        Map<String, VirtualWebhookCarrierIdEnum> map = new HashMap<>();
        map.put("supported-required", VirtualWebhookCarrierIdEnum.SUPPORTED_REQUIRED);
        map.put("supported", VirtualWebhookCarrierIdEnum.SUPPORTED);
        map.put("not-supported", VirtualWebhookCarrierIdEnum.NOT_SUPPORTED);
        return map;
    }
    
    
    public enum VirtualWebhookCarrierIdEnum {

        SUPPORTED_REQUIRED("supported-required"),
        SUPPORTED("supported"),
        NOT_SUPPORTED("not-supported"),;

        private final String value;

        private VirtualWebhookCarrierIdEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

