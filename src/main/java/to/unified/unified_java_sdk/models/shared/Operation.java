/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package to.unified.unified_java_sdk.models.shared;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class Operation {

    public static final Operation ADD = new Operation("add");
    public static final Operation DELETE = new Operation("delete");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, Operation> values = createValuesMap();
    private static final Map<String, OperationEnum> enums = createEnumsMap();

    private final String value;

    private Operation(String value) {
        this.value = value;
    }

    /**
     * Returns a Operation with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as Operation
     */ 
    @JsonCreator
    public static Operation of(String value) {
        synchronized (Operation.class) {
            return values.computeIfAbsent(value, v -> new Operation(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<OperationEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        Operation other = (Operation) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "Operation [value=" + value + "]";
    }

    // return an array just like an enum
    public static Operation[] values() {
        synchronized (Operation.class) {
            return values.values().toArray(new Operation[] {});
        }
    }

    private static final Map<String, Operation> createValuesMap() {
        Map<String, Operation> map = new LinkedHashMap<>();
        map.put("add", ADD);
        map.put("delete", DELETE);
        return map;
    }

    private static final Map<String, OperationEnum> createEnumsMap() {
        Map<String, OperationEnum> map = new HashMap<>();
        map.put("add", OperationEnum.ADD);
        map.put("delete", OperationEnum.DELETE);
        return map;
    }
    
    
    public enum OperationEnum {

        ADD("add"),
        DELETE("delete"),;

        private final String value;

        private OperationEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

