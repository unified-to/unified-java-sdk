/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package to.unified.unified_java_sdk.models.shared;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class ListCallId {

    public static final ListCallId SUPPORTED_REQUIRED = new ListCallId("supported-required");
    public static final ListCallId SUPPORTED = new ListCallId("supported");
    public static final ListCallId NOT_SUPPORTED = new ListCallId("not-supported");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, ListCallId> values = createValuesMap();
    private static final Map<String, ListCallIdEnum> enums = createEnumsMap();

    private final String value;

    private ListCallId(String value) {
        this.value = value;
    }

    /**
     * Returns a ListCallId with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as ListCallId
     */ 
    @JsonCreator
    public static ListCallId of(String value) {
        synchronized (ListCallId.class) {
            return values.computeIfAbsent(value, v -> new ListCallId(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<ListCallIdEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        ListCallId other = (ListCallId) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "ListCallId [value=" + value + "]";
    }

    // return an array just like an enum
    public static ListCallId[] values() {
        synchronized (ListCallId.class) {
            return values.values().toArray(new ListCallId[] {});
        }
    }

    private static final Map<String, ListCallId> createValuesMap() {
        Map<String, ListCallId> map = new LinkedHashMap<>();
        map.put("supported-required", SUPPORTED_REQUIRED);
        map.put("supported", SUPPORTED);
        map.put("not-supported", NOT_SUPPORTED);
        return map;
    }

    private static final Map<String, ListCallIdEnum> createEnumsMap() {
        Map<String, ListCallIdEnum> map = new HashMap<>();
        map.put("supported-required", ListCallIdEnum.SUPPORTED_REQUIRED);
        map.put("supported", ListCallIdEnum.SUPPORTED);
        map.put("not-supported", ListCallIdEnum.NOT_SUPPORTED);
        return map;
    }
    
    
    public enum ListCallIdEnum {

        SUPPORTED_REQUIRED("supported-required"),
        SUPPORTED("supported"),
        NOT_SUPPORTED("not-supported"),;

        private final String value;

        private ListCallIdEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

