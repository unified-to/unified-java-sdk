/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package to.unified.unified_java_sdk.models.shared;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class ListApplicationId {

    public static final ListApplicationId SUPPORTED_REQUIRED = new ListApplicationId("supported-required");
    public static final ListApplicationId SUPPORTED = new ListApplicationId("supported");
    public static final ListApplicationId NOT_SUPPORTED = new ListApplicationId("not-supported");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, ListApplicationId> values = createValuesMap();
    private static final Map<String, ListApplicationIdEnum> enums = createEnumsMap();

    private final String value;

    private ListApplicationId(String value) {
        this.value = value;
    }

    /**
     * Returns a ListApplicationId with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as ListApplicationId
     */ 
    @JsonCreator
    public static ListApplicationId of(String value) {
        synchronized (ListApplicationId.class) {
            return values.computeIfAbsent(value, v -> new ListApplicationId(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<ListApplicationIdEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        ListApplicationId other = (ListApplicationId) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "ListApplicationId [value=" + value + "]";
    }

    // return an array just like an enum
    public static ListApplicationId[] values() {
        synchronized (ListApplicationId.class) {
            return values.values().toArray(new ListApplicationId[] {});
        }
    }

    private static final Map<String, ListApplicationId> createValuesMap() {
        Map<String, ListApplicationId> map = new LinkedHashMap<>();
        map.put("supported-required", SUPPORTED_REQUIRED);
        map.put("supported", SUPPORTED);
        map.put("not-supported", NOT_SUPPORTED);
        return map;
    }

    private static final Map<String, ListApplicationIdEnum> createEnumsMap() {
        Map<String, ListApplicationIdEnum> map = new HashMap<>();
        map.put("supported-required", ListApplicationIdEnum.SUPPORTED_REQUIRED);
        map.put("supported", ListApplicationIdEnum.SUPPORTED);
        map.put("not-supported", ListApplicationIdEnum.NOT_SUPPORTED);
        return map;
    }
    
    
    public enum ListApplicationIdEnum {

        SUPPORTED_REQUIRED("supported-required"),
        SUPPORTED("supported"),
        NOT_SUPPORTED("not-supported"),;

        private final String value;

        private ListApplicationIdEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

