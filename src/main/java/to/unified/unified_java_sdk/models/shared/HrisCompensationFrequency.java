/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package to.unified.unified_java_sdk.models.shared;

import com.fasterxml.jackson.core.JacksonException;
import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.SuppressWarnings;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * <p>Wrapper class for an "open" enum. "Open" enums are those that are expected
 * to evolve (particularly with the addition of enum members over time). If an
 * open enum is used then the appearance of unexpected enum values (say in a 
 * response from an updated an API) will not bring about a runtime error thus 
 * ensuring that non-updated client versions can continue to work without error.
 *
 * <p>Note that instances are immutable and are singletons (an internal thread-safe
 * cache is maintained to ensure that). As a consequence instances created with the 
 * same value will satisfy reference equality (via {@code ==}).
 * 
 * <p>This class is intended to emulate an enum (in terms of common usage and with 
 * reference equality) but with the ability to carry unknown values. Unfortunately
 * Java does not permit the use of an instance in a switch expression but you can 
 * use the {@code asEnum()} method (after dealing with the `Optional` appropriately).
 *
 */
@JsonDeserialize(using = HrisCompensationFrequency._Deserializer.class)
@JsonSerialize(using = HrisCompensationFrequency._Serializer.class)
public class HrisCompensationFrequency {

    public static final HrisCompensationFrequency ONE_TIME = new HrisCompensationFrequency("ONE_TIME");
    public static final HrisCompensationFrequency DAY = new HrisCompensationFrequency("DAY");
    public static final HrisCompensationFrequency QUARTER = new HrisCompensationFrequency("QUARTER");
    public static final HrisCompensationFrequency YEAR = new HrisCompensationFrequency("YEAR");
    public static final HrisCompensationFrequency HOUR = new HrisCompensationFrequency("HOUR");
    public static final HrisCompensationFrequency MONTH = new HrisCompensationFrequency("MONTH");
    public static final HrisCompensationFrequency WEEK = new HrisCompensationFrequency("WEEK");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, HrisCompensationFrequency> values = createValuesMap();
    private static final Map<String, HrisCompensationFrequencyEnum> enums = createEnumsMap();

    private final String value;

    private HrisCompensationFrequency(String value) {
        this.value = value;
    }

    /**
     * Returns a HrisCompensationFrequency with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as HrisCompensationFrequency
     */ 
    public static HrisCompensationFrequency of(String value) {
        synchronized (HrisCompensationFrequency.class) {
            return values.computeIfAbsent(value, v -> new HrisCompensationFrequency(v));
        }
    }

    public String value() {
        return value;
    }

    public Optional<HrisCompensationFrequencyEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        HrisCompensationFrequency other = (HrisCompensationFrequency) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "HrisCompensationFrequency [value=" + value + "]";
    }

    // return an array just like an enum
    public static HrisCompensationFrequency[] values() {
        synchronized (HrisCompensationFrequency.class) {
            return values.values().toArray(new HrisCompensationFrequency[] {});
        }
    }

    private static final Map<String, HrisCompensationFrequency> createValuesMap() {
        Map<String, HrisCompensationFrequency> map = new LinkedHashMap<>();
        map.put("ONE_TIME", ONE_TIME);
        map.put("DAY", DAY);
        map.put("QUARTER", QUARTER);
        map.put("YEAR", YEAR);
        map.put("HOUR", HOUR);
        map.put("MONTH", MONTH);
        map.put("WEEK", WEEK);
        return map;
    }

    private static final Map<String, HrisCompensationFrequencyEnum> createEnumsMap() {
        Map<String, HrisCompensationFrequencyEnum> map = new HashMap<>();
        map.put("ONE_TIME", HrisCompensationFrequencyEnum.ONE_TIME);
        map.put("DAY", HrisCompensationFrequencyEnum.DAY);
        map.put("QUARTER", HrisCompensationFrequencyEnum.QUARTER);
        map.put("YEAR", HrisCompensationFrequencyEnum.YEAR);
        map.put("HOUR", HrisCompensationFrequencyEnum.HOUR);
        map.put("MONTH", HrisCompensationFrequencyEnum.MONTH);
        map.put("WEEK", HrisCompensationFrequencyEnum.WEEK);
        return map;
    }
    
    @SuppressWarnings("serial")
    public static final class _Serializer extends StdSerializer<HrisCompensationFrequency> {

        protected _Serializer() {
            super(HrisCompensationFrequency.class);
        }

        @Override
        public void serialize(HrisCompensationFrequency value, JsonGenerator g, SerializerProvider provider)
                throws IOException, JsonProcessingException {
            g.writeObject(value.value);
        }
    }

    @SuppressWarnings("serial")
    public static final class _Deserializer extends StdDeserializer<HrisCompensationFrequency> {

        protected _Deserializer() {
            super(HrisCompensationFrequency.class);
        }

        @Override
        public HrisCompensationFrequency deserialize(JsonParser p, DeserializationContext ctxt)
                throws IOException, JacksonException {
            String v = p.readValueAs(new TypeReference<String>() {});
            // use the factory method to ensure we get singletons
            return HrisCompensationFrequency.of(v);
        }
    }
    
    public enum HrisCompensationFrequencyEnum {

        ONE_TIME("ONE_TIME"),
        DAY("DAY"),
        QUARTER("QUARTER"),
        YEAR("YEAR"),
        HOUR("HOUR"),
        MONTH("MONTH"),
        WEEK("WEEK"),;

        private final String value;

        private HrisCompensationFrequencyEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

