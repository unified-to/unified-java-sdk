/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package to.unified.unified_java_sdk.models.shared;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class TaxMode {

    public static final TaxMode INCLUSIVE = new TaxMode("INCLUSIVE");
    public static final TaxMode EXCLUSIVE = new TaxMode("EXCLUSIVE");
    public static final TaxMode NONE = new TaxMode("NONE");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, TaxMode> values = createValuesMap();
    private static final Map<String, TaxModeEnum> enums = createEnumsMap();

    private final String value;

    private TaxMode(String value) {
        this.value = value;
    }

    /**
     * Returns a TaxMode with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as TaxMode
     */ 
    @JsonCreator
    public static TaxMode of(String value) {
        synchronized (TaxMode.class) {
            return values.computeIfAbsent(value, v -> new TaxMode(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<TaxModeEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        TaxMode other = (TaxMode) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "TaxMode [value=" + value + "]";
    }

    // return an array just like an enum
    public static TaxMode[] values() {
        synchronized (TaxMode.class) {
            return values.values().toArray(new TaxMode[] {});
        }
    }

    private static final Map<String, TaxMode> createValuesMap() {
        Map<String, TaxMode> map = new LinkedHashMap<>();
        map.put("INCLUSIVE", INCLUSIVE);
        map.put("EXCLUSIVE", EXCLUSIVE);
        map.put("NONE", NONE);
        return map;
    }

    private static final Map<String, TaxModeEnum> createEnumsMap() {
        Map<String, TaxModeEnum> map = new HashMap<>();
        map.put("INCLUSIVE", TaxModeEnum.INCLUSIVE);
        map.put("EXCLUSIVE", TaxModeEnum.EXCLUSIVE);
        map.put("NONE", TaxModeEnum.NONE);
        return map;
    }
    
    
    public enum TaxModeEnum {

        INCLUSIVE("INCLUSIVE"),
        EXCLUSIVE("EXCLUSIVE"),
        NONE("NONE"),;

        private final String value;

        private TaxModeEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

