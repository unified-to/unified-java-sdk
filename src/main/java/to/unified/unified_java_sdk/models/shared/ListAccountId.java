/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package to.unified.unified_java_sdk.models.shared;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class ListAccountId {

    public static final ListAccountId SUPPORTED_REQUIRED = new ListAccountId("supported-required");
    public static final ListAccountId SUPPORTED = new ListAccountId("supported");
    public static final ListAccountId NOT_SUPPORTED = new ListAccountId("not-supported");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, ListAccountId> values = createValuesMap();
    private static final Map<String, ListAccountIdEnum> enums = createEnumsMap();

    private final String value;

    private ListAccountId(String value) {
        this.value = value;
    }

    /**
     * Returns a ListAccountId with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as ListAccountId
     */ 
    @JsonCreator
    public static ListAccountId of(String value) {
        synchronized (ListAccountId.class) {
            return values.computeIfAbsent(value, v -> new ListAccountId(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<ListAccountIdEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        ListAccountId other = (ListAccountId) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "ListAccountId [value=" + value + "]";
    }

    // return an array just like an enum
    public static ListAccountId[] values() {
        synchronized (ListAccountId.class) {
            return values.values().toArray(new ListAccountId[] {});
        }
    }

    private static final Map<String, ListAccountId> createValuesMap() {
        Map<String, ListAccountId> map = new LinkedHashMap<>();
        map.put("supported-required", SUPPORTED_REQUIRED);
        map.put("supported", SUPPORTED);
        map.put("not-supported", NOT_SUPPORTED);
        return map;
    }

    private static final Map<String, ListAccountIdEnum> createEnumsMap() {
        Map<String, ListAccountIdEnum> map = new HashMap<>();
        map.put("supported-required", ListAccountIdEnum.SUPPORTED_REQUIRED);
        map.put("supported", ListAccountIdEnum.SUPPORTED);
        map.put("not-supported", ListAccountIdEnum.NOT_SUPPORTED);
        return map;
    }
    
    
    public enum ListAccountIdEnum {

        SUPPORTED_REQUIRED("supported-required"),
        SUPPORTED("supported"),
        NOT_SUPPORTED("not-supported"),;

        private final String value;

        private ListAccountIdEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

