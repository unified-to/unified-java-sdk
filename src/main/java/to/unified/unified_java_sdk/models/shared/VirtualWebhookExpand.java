/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package to.unified.unified_java_sdk.models.shared;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class VirtualWebhookExpand {

    public static final VirtualWebhookExpand SUPPORTED_REQUIRED = new VirtualWebhookExpand("supported-required");
    public static final VirtualWebhookExpand SUPPORTED = new VirtualWebhookExpand("supported");
    public static final VirtualWebhookExpand NOT_SUPPORTED = new VirtualWebhookExpand("not-supported");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, VirtualWebhookExpand> values = createValuesMap();
    private static final Map<String, VirtualWebhookExpandEnum> enums = createEnumsMap();

    private final String value;

    private VirtualWebhookExpand(String value) {
        this.value = value;
    }

    /**
     * Returns a VirtualWebhookExpand with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as VirtualWebhookExpand
     */ 
    @JsonCreator
    public static VirtualWebhookExpand of(String value) {
        synchronized (VirtualWebhookExpand.class) {
            return values.computeIfAbsent(value, v -> new VirtualWebhookExpand(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<VirtualWebhookExpandEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        VirtualWebhookExpand other = (VirtualWebhookExpand) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "VirtualWebhookExpand [value=" + value + "]";
    }

    // return an array just like an enum
    public static VirtualWebhookExpand[] values() {
        synchronized (VirtualWebhookExpand.class) {
            return values.values().toArray(new VirtualWebhookExpand[] {});
        }
    }

    private static final Map<String, VirtualWebhookExpand> createValuesMap() {
        Map<String, VirtualWebhookExpand> map = new LinkedHashMap<>();
        map.put("supported-required", SUPPORTED_REQUIRED);
        map.put("supported", SUPPORTED);
        map.put("not-supported", NOT_SUPPORTED);
        return map;
    }

    private static final Map<String, VirtualWebhookExpandEnum> createEnumsMap() {
        Map<String, VirtualWebhookExpandEnum> map = new HashMap<>();
        map.put("supported-required", VirtualWebhookExpandEnum.SUPPORTED_REQUIRED);
        map.put("supported", VirtualWebhookExpandEnum.SUPPORTED);
        map.put("not-supported", VirtualWebhookExpandEnum.NOT_SUPPORTED);
        return map;
    }
    
    
    public enum VirtualWebhookExpandEnum {

        SUPPORTED_REQUIRED("supported-required"),
        SUPPORTED("supported"),
        NOT_SUPPORTED("not-supported"),;

        private final String value;

        private VirtualWebhookExpandEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

