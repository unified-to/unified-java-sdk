/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package to.unified.unified_java_sdk.models.shared;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class ListApproverUserId {

    public static final ListApproverUserId SUPPORTED_REQUIRED = new ListApproverUserId("supported-required");
    public static final ListApproverUserId SUPPORTED = new ListApproverUserId("supported");
    public static final ListApproverUserId NOT_SUPPORTED = new ListApproverUserId("not-supported");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, ListApproverUserId> values = createValuesMap();
    private static final Map<String, ListApproverUserIdEnum> enums = createEnumsMap();

    private final String value;

    private ListApproverUserId(String value) {
        this.value = value;
    }

    /**
     * Returns a ListApproverUserId with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as ListApproverUserId
     */ 
    @JsonCreator
    public static ListApproverUserId of(String value) {
        synchronized (ListApproverUserId.class) {
            return values.computeIfAbsent(value, v -> new ListApproverUserId(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<ListApproverUserIdEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        ListApproverUserId other = (ListApproverUserId) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "ListApproverUserId [value=" + value + "]";
    }

    // return an array just like an enum
    public static ListApproverUserId[] values() {
        synchronized (ListApproverUserId.class) {
            return values.values().toArray(new ListApproverUserId[] {});
        }
    }

    private static final Map<String, ListApproverUserId> createValuesMap() {
        Map<String, ListApproverUserId> map = new LinkedHashMap<>();
        map.put("supported-required", SUPPORTED_REQUIRED);
        map.put("supported", SUPPORTED);
        map.put("not-supported", NOT_SUPPORTED);
        return map;
    }

    private static final Map<String, ListApproverUserIdEnum> createEnumsMap() {
        Map<String, ListApproverUserIdEnum> map = new HashMap<>();
        map.put("supported-required", ListApproverUserIdEnum.SUPPORTED_REQUIRED);
        map.put("supported", ListApproverUserIdEnum.SUPPORTED);
        map.put("not-supported", ListApproverUserIdEnum.NOT_SUPPORTED);
        return map;
    }
    
    
    public enum ListApproverUserIdEnum {

        SUPPORTED_REQUIRED("supported-required"),
        SUPPORTED("supported"),
        NOT_SUPPORTED("not-supported"),;

        private final String value;

        private ListApproverUserIdEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

