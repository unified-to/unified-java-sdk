/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package to.unified.unified_java_sdk.models.shared;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class CommerceCollectionType {

    public static final CommerceCollectionType COLLECTION = new CommerceCollectionType("COLLECTION");
    public static final CommerceCollectionType SAVED_SEARCH = new CommerceCollectionType("SAVED_SEARCH");
    public static final CommerceCollectionType CATEGORY = new CommerceCollectionType("CATEGORY");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, CommerceCollectionType> values = createValuesMap();
    private static final Map<String, CommerceCollectionTypeEnum> enums = createEnumsMap();

    private final String value;

    private CommerceCollectionType(String value) {
        this.value = value;
    }

    /**
     * Returns a CommerceCollectionType with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as CommerceCollectionType
     */ 
    @JsonCreator
    public static CommerceCollectionType of(String value) {
        synchronized (CommerceCollectionType.class) {
            return values.computeIfAbsent(value, v -> new CommerceCollectionType(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<CommerceCollectionTypeEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        CommerceCollectionType other = (CommerceCollectionType) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "CommerceCollectionType [value=" + value + "]";
    }

    // return an array just like an enum
    public static CommerceCollectionType[] values() {
        synchronized (CommerceCollectionType.class) {
            return values.values().toArray(new CommerceCollectionType[] {});
        }
    }

    private static final Map<String, CommerceCollectionType> createValuesMap() {
        Map<String, CommerceCollectionType> map = new LinkedHashMap<>();
        map.put("COLLECTION", COLLECTION);
        map.put("SAVED_SEARCH", SAVED_SEARCH);
        map.put("CATEGORY", CATEGORY);
        return map;
    }

    private static final Map<String, CommerceCollectionTypeEnum> createEnumsMap() {
        Map<String, CommerceCollectionTypeEnum> map = new HashMap<>();
        map.put("COLLECTION", CommerceCollectionTypeEnum.COLLECTION);
        map.put("SAVED_SEARCH", CommerceCollectionTypeEnum.SAVED_SEARCH);
        map.put("CATEGORY", CommerceCollectionTypeEnum.CATEGORY);
        return map;
    }
    
    
    public enum CommerceCollectionTypeEnum {

        COLLECTION("COLLECTION"),
        SAVED_SEARCH("SAVED_SEARCH"),
        CATEGORY("CATEGORY"),;

        private final String value;

        private CommerceCollectionTypeEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

