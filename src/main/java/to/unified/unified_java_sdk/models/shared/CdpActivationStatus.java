/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package to.unified.unified_java_sdk.models.shared;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class CdpActivationStatus {

    public static final CdpActivationStatus ACTIVE = new CdpActivationStatus("ACTIVE");
    public static final CdpActivationStatus PAUSED = new CdpActivationStatus("PAUSED");
    public static final CdpActivationStatus ERROR = new CdpActivationStatus("ERROR");
    public static final CdpActivationStatus PENDING = new CdpActivationStatus("PENDING");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, CdpActivationStatus> values = createValuesMap();
    private static final Map<String, CdpActivationStatusEnum> enums = createEnumsMap();

    private final String value;

    private CdpActivationStatus(String value) {
        this.value = value;
    }

    /**
     * Returns a CdpActivationStatus with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as CdpActivationStatus
     */ 
    @JsonCreator
    public static CdpActivationStatus of(String value) {
        synchronized (CdpActivationStatus.class) {
            return values.computeIfAbsent(value, v -> new CdpActivationStatus(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<CdpActivationStatusEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        CdpActivationStatus other = (CdpActivationStatus) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "CdpActivationStatus [value=" + value + "]";
    }

    // return an array just like an enum
    public static CdpActivationStatus[] values() {
        synchronized (CdpActivationStatus.class) {
            return values.values().toArray(new CdpActivationStatus[] {});
        }
    }

    private static final Map<String, CdpActivationStatus> createValuesMap() {
        Map<String, CdpActivationStatus> map = new LinkedHashMap<>();
        map.put("ACTIVE", ACTIVE);
        map.put("PAUSED", PAUSED);
        map.put("ERROR", ERROR);
        map.put("PENDING", PENDING);
        return map;
    }

    private static final Map<String, CdpActivationStatusEnum> createEnumsMap() {
        Map<String, CdpActivationStatusEnum> map = new HashMap<>();
        map.put("ACTIVE", CdpActivationStatusEnum.ACTIVE);
        map.put("PAUSED", CdpActivationStatusEnum.PAUSED);
        map.put("ERROR", CdpActivationStatusEnum.ERROR);
        map.put("PENDING", CdpActivationStatusEnum.PENDING);
        return map;
    }
    
    
    public enum CdpActivationStatusEnum {

        ACTIVE("ACTIVE"),
        PAUSED("PAUSED"),
        ERROR("ERROR"),
        PENDING("PENDING"),;

        private final String value;

        private CdpActivationStatusEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

