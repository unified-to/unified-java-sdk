/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package to.unified.unified_java_sdk.models.shared;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class ListOffset {

    public static final ListOffset SUPPORTED_REQUIRED = new ListOffset("supported-required");
    public static final ListOffset SUPPORTED = new ListOffset("supported");
    public static final ListOffset NOT_SUPPORTED = new ListOffset("not-supported");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, ListOffset> values = createValuesMap();
    private static final Map<String, ListOffsetEnum> enums = createEnumsMap();

    private final String value;

    private ListOffset(String value) {
        this.value = value;
    }

    /**
     * Returns a ListOffset with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as ListOffset
     */ 
    @JsonCreator
    public static ListOffset of(String value) {
        synchronized (ListOffset.class) {
            return values.computeIfAbsent(value, v -> new ListOffset(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<ListOffsetEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        ListOffset other = (ListOffset) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "ListOffset [value=" + value + "]";
    }

    // return an array just like an enum
    public static ListOffset[] values() {
        synchronized (ListOffset.class) {
            return values.values().toArray(new ListOffset[] {});
        }
    }

    private static final Map<String, ListOffset> createValuesMap() {
        Map<String, ListOffset> map = new LinkedHashMap<>();
        map.put("supported-required", SUPPORTED_REQUIRED);
        map.put("supported", SUPPORTED);
        map.put("not-supported", NOT_SUPPORTED);
        return map;
    }

    private static final Map<String, ListOffsetEnum> createEnumsMap() {
        Map<String, ListOffsetEnum> map = new HashMap<>();
        map.put("supported-required", ListOffsetEnum.SUPPORTED_REQUIRED);
        map.put("supported", ListOffsetEnum.SUPPORTED);
        map.put("not-supported", ListOffsetEnum.NOT_SUPPORTED);
        return map;
    }
    
    
    public enum ListOffsetEnum {

        SUPPORTED_REQUIRED("supported-required"),
        SUPPORTED("supported"),
        NOT_SUPPORTED("not-supported"),;

        private final String value;

        private ListOffsetEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

