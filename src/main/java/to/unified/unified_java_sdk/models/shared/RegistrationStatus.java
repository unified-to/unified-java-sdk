/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package to.unified.unified_java_sdk.models.shared;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class RegistrationStatus {

    public static final RegistrationStatus PENDING = new RegistrationStatus("PENDING");
    public static final RegistrationStatus APPROVED = new RegistrationStatus("APPROVED");
    public static final RegistrationStatus REJECTED = new RegistrationStatus("REJECTED");
    public static final RegistrationStatus CANCELLED = new RegistrationStatus("CANCELLED");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, RegistrationStatus> values = createValuesMap();
    private static final Map<String, RegistrationStatusEnum> enums = createEnumsMap();

    private final String value;

    private RegistrationStatus(String value) {
        this.value = value;
    }

    /**
     * Returns a RegistrationStatus with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as RegistrationStatus
     */ 
    @JsonCreator
    public static RegistrationStatus of(String value) {
        synchronized (RegistrationStatus.class) {
            return values.computeIfAbsent(value, v -> new RegistrationStatus(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<RegistrationStatusEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        RegistrationStatus other = (RegistrationStatus) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "RegistrationStatus [value=" + value + "]";
    }

    // return an array just like an enum
    public static RegistrationStatus[] values() {
        synchronized (RegistrationStatus.class) {
            return values.values().toArray(new RegistrationStatus[] {});
        }
    }

    private static final Map<String, RegistrationStatus> createValuesMap() {
        Map<String, RegistrationStatus> map = new LinkedHashMap<>();
        map.put("PENDING", PENDING);
        map.put("APPROVED", APPROVED);
        map.put("REJECTED", REJECTED);
        map.put("CANCELLED", CANCELLED);
        return map;
    }

    private static final Map<String, RegistrationStatusEnum> createEnumsMap() {
        Map<String, RegistrationStatusEnum> map = new HashMap<>();
        map.put("PENDING", RegistrationStatusEnum.PENDING);
        map.put("APPROVED", RegistrationStatusEnum.APPROVED);
        map.put("REJECTED", RegistrationStatusEnum.REJECTED);
        map.put("CANCELLED", RegistrationStatusEnum.CANCELLED);
        return map;
    }
    
    
    public enum RegistrationStatusEnum {

        PENDING("PENDING"),
        APPROVED("APPROVED"),
        REJECTED("REJECTED"),
        CANCELLED("CANCELLED"),;

        private final String value;

        private RegistrationStatusEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

