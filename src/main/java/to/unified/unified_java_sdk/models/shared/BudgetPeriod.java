/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package to.unified.unified_java_sdk.models.shared;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class BudgetPeriod {

    public static final BudgetPeriod DAILY = new BudgetPeriod("DAILY");
    public static final BudgetPeriod MONTHLY = new BudgetPeriod("MONTHLY");
    public static final BudgetPeriod TOTAL = new BudgetPeriod("TOTAL");
    public static final BudgetPeriod LIFETIME = new BudgetPeriod("LIFETIME");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, BudgetPeriod> values = createValuesMap();
    private static final Map<String, BudgetPeriodEnum> enums = createEnumsMap();

    private final String value;

    private BudgetPeriod(String value) {
        this.value = value;
    }

    /**
     * Returns a BudgetPeriod with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as BudgetPeriod
     */ 
    @JsonCreator
    public static BudgetPeriod of(String value) {
        synchronized (BudgetPeriod.class) {
            return values.computeIfAbsent(value, v -> new BudgetPeriod(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<BudgetPeriodEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        BudgetPeriod other = (BudgetPeriod) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "BudgetPeriod [value=" + value + "]";
    }

    // return an array just like an enum
    public static BudgetPeriod[] values() {
        synchronized (BudgetPeriod.class) {
            return values.values().toArray(new BudgetPeriod[] {});
        }
    }

    private static final Map<String, BudgetPeriod> createValuesMap() {
        Map<String, BudgetPeriod> map = new LinkedHashMap<>();
        map.put("DAILY", DAILY);
        map.put("MONTHLY", MONTHLY);
        map.put("TOTAL", TOTAL);
        map.put("LIFETIME", LIFETIME);
        return map;
    }

    private static final Map<String, BudgetPeriodEnum> createEnumsMap() {
        Map<String, BudgetPeriodEnum> map = new HashMap<>();
        map.put("DAILY", BudgetPeriodEnum.DAILY);
        map.put("MONTHLY", BudgetPeriodEnum.MONTHLY);
        map.put("TOTAL", BudgetPeriodEnum.TOTAL);
        map.put("LIFETIME", BudgetPeriodEnum.LIFETIME);
        return map;
    }
    
    
    public enum BudgetPeriodEnum {

        DAILY("DAILY"),
        MONTHLY("MONTHLY"),
        TOTAL("TOTAL"),
        LIFETIME("LIFETIME"),;

        private final String value;

        private BudgetPeriodEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

