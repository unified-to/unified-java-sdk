/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package to.unified.unified_java_sdk.models.shared;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class DurationType {

    public static final DurationType HOUR = new DurationType("HOUR");
    public static final DurationType DAY = new DurationType("DAY");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, DurationType> values = createValuesMap();
    private static final Map<String, DurationTypeEnum> enums = createEnumsMap();

    private final String value;

    private DurationType(String value) {
        this.value = value;
    }

    /**
     * Returns a DurationType with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as DurationType
     */ 
    @JsonCreator
    public static DurationType of(String value) {
        synchronized (DurationType.class) {
            return values.computeIfAbsent(value, v -> new DurationType(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<DurationTypeEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        DurationType other = (DurationType) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "DurationType [value=" + value + "]";
    }

    // return an array just like an enum
    public static DurationType[] values() {
        synchronized (DurationType.class) {
            return values.values().toArray(new DurationType[] {});
        }
    }

    private static final Map<String, DurationType> createValuesMap() {
        Map<String, DurationType> map = new LinkedHashMap<>();
        map.put("HOUR", HOUR);
        map.put("DAY", DAY);
        return map;
    }

    private static final Map<String, DurationTypeEnum> createEnumsMap() {
        Map<String, DurationTypeEnum> map = new HashMap<>();
        map.put("HOUR", DurationTypeEnum.HOUR);
        map.put("DAY", DurationTypeEnum.DAY);
        return map;
    }
    
    
    public enum DurationTypeEnum {

        HOUR("HOUR"),
        DAY("DAY"),;

        private final String value;

        private DurationTypeEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

