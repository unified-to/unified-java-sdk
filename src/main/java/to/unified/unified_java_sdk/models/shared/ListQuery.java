/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package to.unified.unified_java_sdk.models.shared;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class ListQuery {

    public static final ListQuery SUPPORTED_REQUIRED = new ListQuery("supported-required");
    public static final ListQuery SUPPORTED = new ListQuery("supported");
    public static final ListQuery NOT_SUPPORTED = new ListQuery("not-supported");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, ListQuery> values = createValuesMap();
    private static final Map<String, ListQueryEnum> enums = createEnumsMap();

    private final String value;

    private ListQuery(String value) {
        this.value = value;
    }

    /**
     * Returns a ListQuery with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as ListQuery
     */ 
    @JsonCreator
    public static ListQuery of(String value) {
        synchronized (ListQuery.class) {
            return values.computeIfAbsent(value, v -> new ListQuery(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<ListQueryEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        ListQuery other = (ListQuery) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "ListQuery [value=" + value + "]";
    }

    // return an array just like an enum
    public static ListQuery[] values() {
        synchronized (ListQuery.class) {
            return values.values().toArray(new ListQuery[] {});
        }
    }

    private static final Map<String, ListQuery> createValuesMap() {
        Map<String, ListQuery> map = new LinkedHashMap<>();
        map.put("supported-required", SUPPORTED_REQUIRED);
        map.put("supported", SUPPORTED);
        map.put("not-supported", NOT_SUPPORTED);
        return map;
    }

    private static final Map<String, ListQueryEnum> createEnumsMap() {
        Map<String, ListQueryEnum> map = new HashMap<>();
        map.put("supported-required", ListQueryEnum.SUPPORTED_REQUIRED);
        map.put("supported", ListQueryEnum.SUPPORTED);
        map.put("not-supported", ListQueryEnum.NOT_SUPPORTED);
        return map;
    }
    
    
    public enum ListQueryEnum {

        SUPPORTED_REQUIRED("supported-required"),
        SUPPORTED("supported"),
        NOT_SUPPORTED("not-supported"),;

        private final String value;

        private ListQueryEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

