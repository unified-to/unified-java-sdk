/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package to.unified.unified_java_sdk.models.shared;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class Period {

    public static final Period UNSPECIFIED = new Period("UNSPECIFIED");
    public static final Period DAILY = new Period("DAILY");
    public static final Period FLIGHT = new Period("FLIGHT");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, Period> values = createValuesMap();
    private static final Map<String, PeriodEnum> enums = createEnumsMap();

    private final String value;

    private Period(String value) {
        this.value = value;
    }

    /**
     * Returns a Period with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as Period
     */ 
    @JsonCreator
    public static Period of(String value) {
        synchronized (Period.class) {
            return values.computeIfAbsent(value, v -> new Period(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<PeriodEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        Period other = (Period) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "Period [value=" + value + "]";
    }

    // return an array just like an enum
    public static Period[] values() {
        synchronized (Period.class) {
            return values.values().toArray(new Period[] {});
        }
    }

    private static final Map<String, Period> createValuesMap() {
        Map<String, Period> map = new LinkedHashMap<>();
        map.put("UNSPECIFIED", UNSPECIFIED);
        map.put("DAILY", DAILY);
        map.put("FLIGHT", FLIGHT);
        return map;
    }

    private static final Map<String, PeriodEnum> createEnumsMap() {
        Map<String, PeriodEnum> map = new HashMap<>();
        map.put("UNSPECIFIED", PeriodEnum.UNSPECIFIED);
        map.put("DAILY", PeriodEnum.DAILY);
        map.put("FLIGHT", PeriodEnum.FLIGHT);
        return map;
    }
    
    
    public enum PeriodEnum {

        UNSPECIFIED("UNSPECIFIED"),
        DAILY("DAILY"),
        FLIGHT("FLIGHT"),;

        private final String value;

        private PeriodEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

