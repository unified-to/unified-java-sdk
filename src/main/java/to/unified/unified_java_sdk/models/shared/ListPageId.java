/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package to.unified.unified_java_sdk.models.shared;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class ListPageId {

    public static final ListPageId SUPPORTED_REQUIRED = new ListPageId("supported-required");
    public static final ListPageId SUPPORTED = new ListPageId("supported");
    public static final ListPageId NOT_SUPPORTED = new ListPageId("not-supported");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, ListPageId> values = createValuesMap();
    private static final Map<String, ListPageIdEnum> enums = createEnumsMap();

    private final String value;

    private ListPageId(String value) {
        this.value = value;
    }

    /**
     * Returns a ListPageId with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as ListPageId
     */ 
    @JsonCreator
    public static ListPageId of(String value) {
        synchronized (ListPageId.class) {
            return values.computeIfAbsent(value, v -> new ListPageId(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<ListPageIdEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        ListPageId other = (ListPageId) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "ListPageId [value=" + value + "]";
    }

    // return an array just like an enum
    public static ListPageId[] values() {
        synchronized (ListPageId.class) {
            return values.values().toArray(new ListPageId[] {});
        }
    }

    private static final Map<String, ListPageId> createValuesMap() {
        Map<String, ListPageId> map = new LinkedHashMap<>();
        map.put("supported-required", SUPPORTED_REQUIRED);
        map.put("supported", SUPPORTED);
        map.put("not-supported", NOT_SUPPORTED);
        return map;
    }

    private static final Map<String, ListPageIdEnum> createEnumsMap() {
        Map<String, ListPageIdEnum> map = new HashMap<>();
        map.put("supported-required", ListPageIdEnum.SUPPORTED_REQUIRED);
        map.put("supported", ListPageIdEnum.SUPPORTED);
        map.put("not-supported", ListPageIdEnum.NOT_SUPPORTED);
        return map;
    }
    
    
    public enum ListPageIdEnum {

        SUPPORTED_REQUIRED("supported-required"),
        SUPPORTED("supported"),
        NOT_SUPPORTED("not-supported"),;

        private final String value;

        private ListPageIdEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

