/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package to.unified.unified_java_sdk.models.shared;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class ComputeMode {

    public static final ComputeMode REALTIME = new ComputeMode("REALTIME");
    public static final ComputeMode BATCH = new ComputeMode("BATCH");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, ComputeMode> values = createValuesMap();
    private static final Map<String, ComputeModeEnum> enums = createEnumsMap();

    private final String value;

    private ComputeMode(String value) {
        this.value = value;
    }

    /**
     * Returns a ComputeMode with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as ComputeMode
     */ 
    @JsonCreator
    public static ComputeMode of(String value) {
        synchronized (ComputeMode.class) {
            return values.computeIfAbsent(value, v -> new ComputeMode(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<ComputeModeEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        ComputeMode other = (ComputeMode) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "ComputeMode [value=" + value + "]";
    }

    // return an array just like an enum
    public static ComputeMode[] values() {
        synchronized (ComputeMode.class) {
            return values.values().toArray(new ComputeMode[] {});
        }
    }

    private static final Map<String, ComputeMode> createValuesMap() {
        Map<String, ComputeMode> map = new LinkedHashMap<>();
        map.put("REALTIME", REALTIME);
        map.put("BATCH", BATCH);
        return map;
    }

    private static final Map<String, ComputeModeEnum> createEnumsMap() {
        Map<String, ComputeModeEnum> map = new HashMap<>();
        map.put("REALTIME", ComputeModeEnum.REALTIME);
        map.put("BATCH", ComputeModeEnum.BATCH);
        return map;
    }
    
    
    public enum ComputeModeEnum {

        REALTIME("REALTIME"),
        BATCH("BATCH"),;

        private final String value;

        private ComputeModeEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

