/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package to.unified.unified_java_sdk.models.shared;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class ListPaymentId {

    public static final ListPaymentId SUPPORTED_REQUIRED = new ListPaymentId("supported-required");
    public static final ListPaymentId SUPPORTED = new ListPaymentId("supported");
    public static final ListPaymentId NOT_SUPPORTED = new ListPaymentId("not-supported");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, ListPaymentId> values = createValuesMap();
    private static final Map<String, ListPaymentIdEnum> enums = createEnumsMap();

    private final String value;

    private ListPaymentId(String value) {
        this.value = value;
    }

    /**
     * Returns a ListPaymentId with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as ListPaymentId
     */ 
    @JsonCreator
    public static ListPaymentId of(String value) {
        synchronized (ListPaymentId.class) {
            return values.computeIfAbsent(value, v -> new ListPaymentId(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<ListPaymentIdEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        ListPaymentId other = (ListPaymentId) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "ListPaymentId [value=" + value + "]";
    }

    // return an array just like an enum
    public static ListPaymentId[] values() {
        synchronized (ListPaymentId.class) {
            return values.values().toArray(new ListPaymentId[] {});
        }
    }

    private static final Map<String, ListPaymentId> createValuesMap() {
        Map<String, ListPaymentId> map = new LinkedHashMap<>();
        map.put("supported-required", SUPPORTED_REQUIRED);
        map.put("supported", SUPPORTED);
        map.put("not-supported", NOT_SUPPORTED);
        return map;
    }

    private static final Map<String, ListPaymentIdEnum> createEnumsMap() {
        Map<String, ListPaymentIdEnum> map = new HashMap<>();
        map.put("supported-required", ListPaymentIdEnum.SUPPORTED_REQUIRED);
        map.put("supported", ListPaymentIdEnum.SUPPORTED);
        map.put("not-supported", ListPaymentIdEnum.NOT_SUPPORTED);
        return map;
    }
    
    
    public enum ListPaymentIdEnum {

        SUPPORTED_REQUIRED("supported-required"),
        SUPPORTED("supported"),
        NOT_SUPPORTED("not-supported"),;

        private final String value;

        private ListPaymentIdEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

