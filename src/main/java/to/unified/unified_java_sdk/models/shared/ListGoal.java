/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package to.unified.unified_java_sdk.models.shared;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class ListGoal {

    public static final ListGoal SUPPORTED_REQUIRED = new ListGoal("supported-required");
    public static final ListGoal SUPPORTED = new ListGoal("supported");
    public static final ListGoal NOT_SUPPORTED = new ListGoal("not-supported");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, ListGoal> values = createValuesMap();
    private static final Map<String, ListGoalEnum> enums = createEnumsMap();

    private final String value;

    private ListGoal(String value) {
        this.value = value;
    }

    /**
     * Returns a ListGoal with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as ListGoal
     */ 
    @JsonCreator
    public static ListGoal of(String value) {
        synchronized (ListGoal.class) {
            return values.computeIfAbsent(value, v -> new ListGoal(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<ListGoalEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        ListGoal other = (ListGoal) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "ListGoal [value=" + value + "]";
    }

    // return an array just like an enum
    public static ListGoal[] values() {
        synchronized (ListGoal.class) {
            return values.values().toArray(new ListGoal[] {});
        }
    }

    private static final Map<String, ListGoal> createValuesMap() {
        Map<String, ListGoal> map = new LinkedHashMap<>();
        map.put("supported-required", SUPPORTED_REQUIRED);
        map.put("supported", SUPPORTED);
        map.put("not-supported", NOT_SUPPORTED);
        return map;
    }

    private static final Map<String, ListGoalEnum> createEnumsMap() {
        Map<String, ListGoalEnum> map = new HashMap<>();
        map.put("supported-required", ListGoalEnum.SUPPORTED_REQUIRED);
        map.put("supported", ListGoalEnum.SUPPORTED);
        map.put("not-supported", ListGoalEnum.NOT_SUPPORTED);
        return map;
    }
    
    
    public enum ListGoalEnum {

        SUPPORTED_REQUIRED("supported-required"),
        SUPPORTED("supported"),
        NOT_SUPPORTED("not-supported"),;

        private final String value;

        private ListGoalEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

