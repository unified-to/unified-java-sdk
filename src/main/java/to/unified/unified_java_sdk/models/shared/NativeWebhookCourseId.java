/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package to.unified.unified_java_sdk.models.shared;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class NativeWebhookCourseId {

    public static final NativeWebhookCourseId SUPPORTED_REQUIRED = new NativeWebhookCourseId("supported-required");
    public static final NativeWebhookCourseId SUPPORTED = new NativeWebhookCourseId("supported");
    public static final NativeWebhookCourseId NOT_SUPPORTED = new NativeWebhookCourseId("not-supported");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, NativeWebhookCourseId> values = createValuesMap();
    private static final Map<String, NativeWebhookCourseIdEnum> enums = createEnumsMap();

    private final String value;

    private NativeWebhookCourseId(String value) {
        this.value = value;
    }

    /**
     * Returns a NativeWebhookCourseId with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as NativeWebhookCourseId
     */ 
    @JsonCreator
    public static NativeWebhookCourseId of(String value) {
        synchronized (NativeWebhookCourseId.class) {
            return values.computeIfAbsent(value, v -> new NativeWebhookCourseId(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<NativeWebhookCourseIdEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        NativeWebhookCourseId other = (NativeWebhookCourseId) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "NativeWebhookCourseId [value=" + value + "]";
    }

    // return an array just like an enum
    public static NativeWebhookCourseId[] values() {
        synchronized (NativeWebhookCourseId.class) {
            return values.values().toArray(new NativeWebhookCourseId[] {});
        }
    }

    private static final Map<String, NativeWebhookCourseId> createValuesMap() {
        Map<String, NativeWebhookCourseId> map = new LinkedHashMap<>();
        map.put("supported-required", SUPPORTED_REQUIRED);
        map.put("supported", SUPPORTED);
        map.put("not-supported", NOT_SUPPORTED);
        return map;
    }

    private static final Map<String, NativeWebhookCourseIdEnum> createEnumsMap() {
        Map<String, NativeWebhookCourseIdEnum> map = new HashMap<>();
        map.put("supported-required", NativeWebhookCourseIdEnum.SUPPORTED_REQUIRED);
        map.put("supported", NativeWebhookCourseIdEnum.SUPPORTED);
        map.put("not-supported", NativeWebhookCourseIdEnum.NOT_SUPPORTED);
        return map;
    }
    
    
    public enum NativeWebhookCourseIdEnum {

        SUPPORTED_REQUIRED("supported-required"),
        SUPPORTED("supported"),
        NOT_SUPPORTED("not-supported"),;

        private final String value;

        private NativeWebhookCourseIdEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

