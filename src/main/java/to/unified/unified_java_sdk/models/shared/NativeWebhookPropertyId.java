/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package to.unified.unified_java_sdk.models.shared;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class NativeWebhookPropertyId {

    public static final NativeWebhookPropertyId SUPPORTED_REQUIRED = new NativeWebhookPropertyId("supported-required");
    public static final NativeWebhookPropertyId SUPPORTED = new NativeWebhookPropertyId("supported");
    public static final NativeWebhookPropertyId NOT_SUPPORTED = new NativeWebhookPropertyId("not-supported");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, NativeWebhookPropertyId> values = createValuesMap();
    private static final Map<String, NativeWebhookPropertyIdEnum> enums = createEnumsMap();

    private final String value;

    private NativeWebhookPropertyId(String value) {
        this.value = value;
    }

    /**
     * Returns a NativeWebhookPropertyId with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as NativeWebhookPropertyId
     */ 
    @JsonCreator
    public static NativeWebhookPropertyId of(String value) {
        synchronized (NativeWebhookPropertyId.class) {
            return values.computeIfAbsent(value, v -> new NativeWebhookPropertyId(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<NativeWebhookPropertyIdEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        NativeWebhookPropertyId other = (NativeWebhookPropertyId) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "NativeWebhookPropertyId [value=" + value + "]";
    }

    // return an array just like an enum
    public static NativeWebhookPropertyId[] values() {
        synchronized (NativeWebhookPropertyId.class) {
            return values.values().toArray(new NativeWebhookPropertyId[] {});
        }
    }

    private static final Map<String, NativeWebhookPropertyId> createValuesMap() {
        Map<String, NativeWebhookPropertyId> map = new LinkedHashMap<>();
        map.put("supported-required", SUPPORTED_REQUIRED);
        map.put("supported", SUPPORTED);
        map.put("not-supported", NOT_SUPPORTED);
        return map;
    }

    private static final Map<String, NativeWebhookPropertyIdEnum> createEnumsMap() {
        Map<String, NativeWebhookPropertyIdEnum> map = new HashMap<>();
        map.put("supported-required", NativeWebhookPropertyIdEnum.SUPPORTED_REQUIRED);
        map.put("supported", NativeWebhookPropertyIdEnum.SUPPORTED);
        map.put("not-supported", NativeWebhookPropertyIdEnum.NOT_SUPPORTED);
        return map;
    }
    
    
    public enum NativeWebhookPropertyIdEnum {

        SUPPORTED_REQUIRED("supported-required"),
        SUPPORTED("supported"),
        NOT_SUPPORTED("not-supported"),;

        private final String value;

        private NativeWebhookPropertyIdEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

