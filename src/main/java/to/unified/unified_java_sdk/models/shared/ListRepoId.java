/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package to.unified.unified_java_sdk.models.shared;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class ListRepoId {

    public static final ListRepoId SUPPORTED_REQUIRED = new ListRepoId("supported-required");
    public static final ListRepoId SUPPORTED = new ListRepoId("supported");
    public static final ListRepoId NOT_SUPPORTED = new ListRepoId("not-supported");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, ListRepoId> values = createValuesMap();
    private static final Map<String, ListRepoIdEnum> enums = createEnumsMap();

    private final String value;

    private ListRepoId(String value) {
        this.value = value;
    }

    /**
     * Returns a ListRepoId with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as ListRepoId
     */ 
    @JsonCreator
    public static ListRepoId of(String value) {
        synchronized (ListRepoId.class) {
            return values.computeIfAbsent(value, v -> new ListRepoId(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<ListRepoIdEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        ListRepoId other = (ListRepoId) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "ListRepoId [value=" + value + "]";
    }

    // return an array just like an enum
    public static ListRepoId[] values() {
        synchronized (ListRepoId.class) {
            return values.values().toArray(new ListRepoId[] {});
        }
    }

    private static final Map<String, ListRepoId> createValuesMap() {
        Map<String, ListRepoId> map = new LinkedHashMap<>();
        map.put("supported-required", SUPPORTED_REQUIRED);
        map.put("supported", SUPPORTED);
        map.put("not-supported", NOT_SUPPORTED);
        return map;
    }

    private static final Map<String, ListRepoIdEnum> createEnumsMap() {
        Map<String, ListRepoIdEnum> map = new HashMap<>();
        map.put("supported-required", ListRepoIdEnum.SUPPORTED_REQUIRED);
        map.put("supported", ListRepoIdEnum.SUPPORTED);
        map.put("not-supported", ListRepoIdEnum.NOT_SUPPORTED);
        return map;
    }
    
    
    public enum ListRepoIdEnum {

        SUPPORTED_REQUIRED("supported-required"),
        SUPPORTED("supported"),
        NOT_SUPPORTED("not-supported"),;

        private final String value;

        private ListRepoIdEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

